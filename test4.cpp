#include<iostream>
using namespace std;
int main ()
{
	// follows the bodmas
	
	double u = 2;
	cout<< u; //The variable does store 2.0 (a floating-point number), but cout prints floating-point numbers in the shortest readable form by default.
	// note: double has 8 bytes of memory so for presision calculation we use double.
	// if we calculate integer with integer we get integer value. most imp point.
	//
	return 0;
}